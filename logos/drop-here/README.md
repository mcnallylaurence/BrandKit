# Drop more logos here

Put extra files in this folder:

- SVG or PDF vectors
- Print-ready CMYK files
- Horizontal lockups we do not have yet
- Favicon / app icon masters

Keep the file name plain, like `Primary_Green_Shamrock.svg`. Then ask Cursor to move it into `logos/` and update [../README.md](../README.md).
